(define setup (lambda () (begin
  (cls)
  
  (let ((i 0) (j 0))
    (while (< j 16) (begin
      (set! i 0)
      (while (< i 16) (begin
        (text (chr (+ (* j 16) i)) (* i 8) (* j 8) (color 255 255 255))
        (set! i (+ i 1))
      ))
      (set! j (+ j 1))
    ))
  )
)))

(define update (lambda (dt) (begin
)))
